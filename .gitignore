# .gitignore
# npm 依赖包（无需提交，可通过 npm install 重新安装）
node_modules/
# npm 锁文件（可选忽略，新手建议保留 package-lock.json）
# package-lock.json
# 日志文件
*.log
# 编辑器配置（VSCode 示例）
.vscode/
# 操作系统临时文件
.DS_Store
Thumbs.db
# GitLab CI 生成的临时目录
public/
# 个人测试用的临时文件
tmp/